# MCP Server 개발 요구사항 명세서

## 기본 정보
- **서버명:** [kebab-case]
- **연결 대상:** [외부 도구/서비스명]
- **필요 사유:** [어떤 플러그인의 어떤 기능에 필요한지]
- **우선순위:** [필수 / 선택]

## 연결 대상 정보
- **API 문서 URL:** [수집된 경우 기재]
- **인증 방식:** [API Key / OAuth / Basic Auth / 미확인]
- **Base URL:** [확인된 경우 기재]

## 필요 Tools 목록
| Tool명 | 설명 | 입력 | 출력 |
|--------|------|------|------|
| [tool-name] | [기능] | [파라미터] | [반환값] |

## 환경 변수
| 변수명 | 용도 |
|--------|------|
| [VAR_NAME] | [설명] |

**Plugin Builder**: API 키 등은 프로젝트 루트 `.env.mcp`에만 정의한다. `.cursor/mcp.json`에는 env 값을 넣지 않고, envmcp를 사용해 `.env.mcp`와 연결한다. `.env.mcp`가 없으면 터미널에서 `npm install -g envmcp` 실행 후 `/setup-mcp-env`로 템플릿 생성.

## 참고 사항
- [추가 기술적 참고 사항]
- [유사 MCP 서버 참조 링크 (있는 경우)]
